terraform {
  backend "azurerm" {
    esource_group_name  = "rg-ProjectcoordinatorDeploy-prd"
    storage_account_name = "rg-ProjectcoordinatorDeploy-prd"
    container_name       = "tfstate"
    key                  = "projectcoordinator.tfstate"
    use_azuread_auth     = true
    use_oidc             = true
  }
}
